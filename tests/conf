# Shim configuration file
# Uncomment and change any of the following values. Restart shim for
# your changes to take effect (default values are shown). See
# man shim
# for more information on the options.

ports=8088,8089s
doc=/tmp/shim/wwwroot
#scidbhost=localhost
#scidbport=$PORT
#instance=$INS
#tmp=$TMP
#user=$SCIDBUSER
#max_sessions=50
#timeout=60
aio=1
